export interface statsTypes{
    appointmentsCount:number
}

export interface ClientTypes{
    id: string;
    name: string;
    dob: string;
    phone: string;
    selectedValue: string;
    date: string;
}